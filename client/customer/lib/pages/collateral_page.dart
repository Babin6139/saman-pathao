import 'package:flutter/material.dart';

class CollateralPage extends StatelessWidget {
  const CollateralPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
